apiVersion: v1
kind: Service
metadata:
  name: postgres-order
  namespace: delivery-system
spec:
  type: ClusterIP
  selector:
    app: postgres-order
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
